{
  "identifier": "default",
  "description": "Default capabilities for the KeyKeyKey desktop app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "mcp-bridge:default"
  ]
}
